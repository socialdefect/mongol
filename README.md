mongol
======

First attempts towards a cross platform webapplication framework for desktop and mobile devices using HTML5 and BASH/Perl CGI. The goal is to set-up an easy to use semi-automated system for developing tiny web applications that should be able to run in one single click on every OS and every architecture without the need for any plug-ins or dependencies on the client side.  
